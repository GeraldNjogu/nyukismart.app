document.addEventListener('DOMContentLoaded', () => {
    const helpBtn = document.getElementById('loginBtn');
    loginBtn.addEventListener('click', () => {
        window.location.href = 'login.html'; // Replace 'help.html' with the path to your help page
    });
});
document.addEventListener('DOMContentLoaded', () => {
    const helpBtn = document.getElementById('signupBtn');
    signupBtn.addEventListener('click', () => {
        window.location.href = 'sign up.html'; // Replace 'help.html' with the path to your help page
    });
});




document.addEventListener('DOMContentLoaded', () => {
    const helpBtn = document.getElementById('helpBtn');
    helpBtn.addEventListener('click', () => {
        window.location.href = 'help.html'; // Replace 'help.html' with the path to your help page
    });
});

